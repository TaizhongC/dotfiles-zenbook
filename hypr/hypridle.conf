# Lock whenever systemd prepares to suspend (lid close, suspend key, or menu).
# No idle listeners are defined: this does not auto-lock while you are working.
general {
    lock_cmd = pidof hyprlock || hyprlock
    before_sleep_cmd = loginctl lock-session
    inhibit_sleep = 3
}
