#!/usr/bin/env sh
status=$(wpctl get-volume @DEFAULT_AUDIO_SOURCE@ 2>/dev/null) || exit 0
case "$status" in
  *MUTED*) printf '%s\n' '{"text":"","class":"muted"}' ;;
  *) printf '%s\n' '{"text":"","class":"microphone"}' ;;
esac
